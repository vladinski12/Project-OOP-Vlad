// Proiect POO.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

/*
Minim 3 clase ce au legătură cu proiectul
Clasele trebuie să conțină(per total, nu fiecare) 
	cel puțin un vector alocat dinamic de caractere,														X int** locuri - Locatie.h
	un vector numeric alocat dinamic,																		X char* denumire - Eveniment.h
	un câmp constant,																						X const long id - Bilet.h
	un câmp static																							X static double bileteTotale  static double bileteVandute  static double bileteDisponibile - Bilete.h
	și o metodă statică.																					X static bool checkBilete() - Bilete.h
Membrii trebuie să aibă legătură cu clasa.																	X
Toate atributele vor fi definite în zona privată a clasei													X
Toate atributele vor avea metode de acces; setterii vor conține validări									X
Fiecare clasa va conține cel puțin 2 metode generice(altele decât constructorii sau metodele de acces)
	ce vor fi utilizate pentru a realiza diverse prelucrări													?
Fiecare clasă va conține cel puțin un constructor implicit și unul cu parametri.
	Clasele cu membri pointeri vor respecta „regula celor 3”.												X
Fiecare clasă va conține supraîncărcări pentru operatorii << și >>											X
Fiecare clasă va avea cel puțin 2 operatori supraîncărcați dintre următorii									X
	(același operator nu poate fi supraîncărcat în 2 clase diferite) :								
		operator de indexare[]																				X
		operatori aritmetici(+, -, *or /)																	X
		++ or --(ambele forme)																				X
		cast implicit sau explicit
		operator de negație !
		operatori relaționali(<.>, = < , >= , == )															X
Clasele vor fi implementate în propriul fișier header și / sau cpp											X
Funcția main va fi localizată într - un alt fișier cpp														X
Această fază este considerată implementată dacă cel puțin 75 % dintre cerințe sunt implementate
*/

#include <iostream>
#include <string>
#include "Eveniment.h"
#include "Locatie.h"
#include "Bilete.h"
using namespace std;



int main()
{
	return 0;
}
